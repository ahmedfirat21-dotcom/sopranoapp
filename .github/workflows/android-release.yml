name: Build Android release bundle and test APK

on:
  workflow_dispatch:
  push:
    branches:
      - fix/android-16-release

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 16 SDK
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Install dependencies
        run: npm ci

      - name: Build unsigned release bundle and installable test APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew :app:bundleRelease :app:assembleDebug --no-daemon

      - name: Upload unsigned bundle
        uses: actions/upload-artifact@v4
        with:
          name: sopranochat-android-1.3.53-unsigned
          path: android/app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error
          retention-days: 7

      - name: Upload installable test APK
        uses: actions/upload-artifact@v4
        with:
          name: sopranochat-android-1.3.53-test-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 7
